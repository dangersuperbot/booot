do

function run(msg, matches)
  return [[
#Sudoers!
  
  1.@xXDangerXx🌐 #Developer ( use #adddeveloper command to add #xXDangerXx in your Groups & SuperGroups. )
  
  2.@senator_blackk🌐 #Manager ( use #addmanager command to add #senator in your Groups & SuperGroups. )
  
]]
end
return {
  description = " ", 
  usage = " ",
  patterns = {
    "^[#!/]sudoers$",

  },
  run = run
}
end
